# Depression-Prediction-ML
Built a Teen Depression Prediction system using Machine Learning by evaluating Logistic Regression, Decision Tree, Random Forest, KNN, and SVM. After comparing performance, Logistic Regression was selected as the final model and deployed with a Streamlit web application for real-time prediction.
